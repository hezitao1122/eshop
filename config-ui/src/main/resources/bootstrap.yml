server:
  port: 58888
spring:
  application:
    name: amap-config-ui
  cloud:
    config:
      # config的绝对地址,如果不适用绝对地址,则需要引入eureka的配置
      uri: http://localhost:58013/amap-config
      name: ${spring.application.name}
      profile: ${spring.profiles.active}
      label: master
  profiles:
    active: dev
